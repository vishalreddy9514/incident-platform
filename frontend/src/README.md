Application source (components, routes, API client, TanStack Query hooks)
is built in Phase 7 - React/TypeScript frontend. Tooling (ESLint, Prettier,
TypeScript, Vite, Vitest) is configured now so standards are established
before feature code lands.
