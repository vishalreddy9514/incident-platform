# Copy to backend.hcl (git-ignored) with the real bucket/table names the
# bootstrap config (../../bootstrap) created, then:
#   terraform init -backend-config=backend.hcl

bucket         = "incident-platform-terraform-state"
region         = "eu-west-2"
dynamodb_table = "incident-platform-terraform-locks"
