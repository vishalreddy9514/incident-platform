# API Documentation

> **Status:** stub. The authoritative API contract is the generated OpenAPI
> spec (springdoc-openapi for the backend, FastAPI's native OpenAPI for the
> AI service) so it can never drift from the code. This document will link
> to those generated specs and add narrative context (auth flow, pagination
> conventions, error envelope shape) once the backend exists (Phase 4+).
>
> Endpoint list as currently planned: see
> [`PHASE-1-requirements-and-architecture.md`](../PHASE-1-requirements-and-architecture.md) §9.
