# Runtime dependencies only. Dev, test and lint tooling lives in
# requirements-dev.txt, and the Dockerfile installs this file alone, so the
# production image no longer ships pytest, ruff, black and mypy — nor their
# CVEs, which were failing the deploy pipeline's image scan for code that
# never runs in production (CVE-2026-32274 in black).
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0
httpx==0.28.1
prometheus-fastapi-instrumentator==8.1.0

# Transitive via fastapi and the instrumentator, pinned here on purpose:
# starlette below 1.3.1 carries CVE-2026-48818 (SSRF via UNC paths in
# StaticFiles) and CVE-2026-54283 (form limits silently ignored). fastapi
# asks only for >=0.46.0 and the instrumentator for >=1.0.0, so without
# this line a fresh resolve is free to land back on a vulnerable version.
starlette==1.6.0
