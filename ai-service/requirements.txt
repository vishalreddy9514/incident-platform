# Runtime
fastapi==0.115.0
uvicorn[standard]==0.31.0
pydantic==2.9.2
pydantic-settings==2.5.2
httpx==0.27.2
prometheus-fastapi-instrumentator==7.1.0

# Dev / test / lint (kept in the same file for a project this size;
# a requirements-dev.txt split is a reasonable future improvement if
# the dependency list grows).
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
ruff==0.6.9
black==24.10.0
mypy==1.11.2
