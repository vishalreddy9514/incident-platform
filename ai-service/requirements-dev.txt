# Dev, test and lint tooling. Installed in CI and locally, never in the
# runtime image — see requirements.txt's header.
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
ruff==0.16.8
black==26.5.1
mypy==2.3.1
