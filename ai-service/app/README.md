Application code (FastAPI app, routers, services, provider abstraction) is
built in Phase 8 - Python AI microservice. This package is scaffolded now
so the project structure, linting, and CI config can be set up against it
in earlier phases without a placeholder/fake implementation being required.
